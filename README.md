# Sanjay Portfolio

Open `index.html` in a browser to view the portfolio.

To use your actual photo in the animated name, place a square or portrait image named `profile-photo.jpg` inside the `assets` folder. The hover effect will then automatically fill every letter of your name with that photo.
